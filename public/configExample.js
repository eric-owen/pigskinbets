//Create a file called config.js INSIDE the public folder. Copy and paste the below object into your config.js file and replace INSERT-KEY-HERE with your key. 

const config = {
    API_Key : 'INSERT-KEY-HERE'
};